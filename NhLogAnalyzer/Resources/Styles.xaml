<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:ae="http://icsharpcode.net/sharpdevelop/avalonedit">
	<SolidColorBrush x:Key="BackgroundBrush" Color="#282828" />
	<SolidColorBrush x:Key="ForegroundBrush" Color="White" />

	<Style x:Key="DefaultControlStyle" TargetType="{x:Type Control}">
		<Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
		<Setter Property="FontFamily" Value="Consolas" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="FontWeight" Value="Normal" />
		<Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
	</Style>
	
	<Style x:Key="SqlEditorStyle" BasedOn="{StaticResource DefaultControlStyle}" TargetType="{x:Type ae:TextEditor}">
		<Setter Property="FontSize" Value="18" />
	</Style>

	<Style x:Key="StackTraceStyle" BasedOn="{StaticResource DefaultControlStyle}" TargetType="{x:Type TextBox}">
	</Style>

	<Style TargetType="{x:Type Window}">
		<Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
		<Setter Property="OverridesDefaultStyle" Value="True" />
	</Style>

	<Style TargetType="{x:Type GridSplitter}">
		<Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
	</Style>
</ResourceDictionary>