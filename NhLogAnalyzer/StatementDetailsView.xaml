<UserControl x:Class="NhLogAnalyzer.StatementDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:ae="http://icsharpcode.net/sharpdevelop/avalonedit"
			 xmlns:local="clr-namespace:NhLogAnalyzer"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
	<Grid Grid.Column="2">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="3" />
			<RowDefinition Height="250" />
		</Grid.RowDefinitions>
		<ae:TextEditor Grid.Row="0"
					   Document="{Binding Document}"
					   Style="{StaticResource SqlEditorStyle}"
					   SyntaxHighlighting="{Binding Source='SqlSyntaxDefinition', Converter={x:Static local:XshdConverter.Default}}" />
		<GridSplitter Grid.Row="1" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
		<TextBox Grid.Row="2"
				 Style="{StaticResource StackTraceStyle}"
				 Text="{Binding Statement.StackTrace, Mode=OneWay}" />
	</Grid>
</UserControl>
